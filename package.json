{
  "name": "web-checker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Warning: no test specified\" && exit 0",
    "build": "mkdir -p dist && esbuild src/index.js --bundle --platform=node --target=node20 --external:@sparticuz/chromium --outfile=dist/index.js && cp -r node_modules dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.975.0",
    "@aws-sdk/client-sns": "^3.980.0",
    "@sparticuz/chromium": "^143.0.4",
    "pino": "^10.3.0",
    "puppeteer-core": "^24.36.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3",
    "esbuild": "^0.25.0"
  }
}